# Minimal requirements for evaluation + light EuroLLM fine-tuning
# For dual RTX 3090 setup with CUDA 12.4

# Core packages (will be installed with CUDA support separately)
# torch, torchvision, torchaudio

# Transformers and training
transformers>=4.38.0
accelerate>=1.0.1
datasets>=2.16.0
trl>=0.7.10
peft>=0.8.0
sentencepiece>=0.1.99

# Evaluation metrics
evaluate>=0.4.1
scikit-learn>=1.3.0
nltk>=3.8.1
textstat>=0.7.3
scipy>=1.10.0
sacrebleu>=2.5.0
sacremoses>=0.1.0

# Utilities
pyyaml>=6.0
tqdm>=4.65.0
python-dotenv>=1.0.0

# Optional: Experiment tracking
wandb>=0.16.0
tensorboard>=2.15.0

# Note: bitsandbytes excluded (not needed for eval, may cause issues)
# Note: flash-attn can be installed separately if needed
